from django.apps import AppConfig


class PramsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.prams'
    verbose_name = 'PRAMS (Participant Record and Management System)'
